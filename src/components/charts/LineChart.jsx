// Komponen Grafik Historis (Suhu, pH, Gas, RSSI, Delay)
import React from 'react';

export const LineChart = ({ data, title }) => (
  <div>
    <h4>{title}</h4>
    {/* Visualisasi Grafik */}
  </div>
);
